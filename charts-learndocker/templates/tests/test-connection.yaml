apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "charts-learndocker.fullname" . }}-test-connection"
  labels:
    {{- include "charts-learndocker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "charts-learndocker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
